---
interface Props {
  id?: string;
  className?: string;
}

const { id, className } = Astro.props;
---

<section
  id={id}
  class={`min-h-screen flex flex-col justify-center items-center text-center p-4 sm:p-8 max-w-4xl mx-auto ${className}`}
>
  <slot />
</section>
